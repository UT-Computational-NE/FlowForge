/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  12
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    format      binary;
    class       volScalarField;
    location    "0";
    object      alpha.argon;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 0 0 0 0 0 0];

internalField   uniform 0;

boundaryField
{
    inlet
    {
        type            codedFixedValue;
        value           uniform 0;
        name            circularAlphaInletArgon;
        code            #{
            const vectorField& Cf = this->patch().Cf(); // Face centers
            scalarField alpha(this->size(), 0);        // Initialize all to 0
            const scalar R2 = sqr({radius});                // Radius^2 = 1e-6
            // New center location (negative means left bound)
            const scalar x0 = -{centroid};
            const scalar z0 = 0.0000;


            forAll(Cf, faceI)
            {
                const vector& c = Cf[faceI];
                scalar r2 = sqr(c.x() - x0) + sqr(c.z() - z0);     // Distance from center (x,z)

                if (r2 {inequal} R2)
                    alpha[faceI] = 1.0;                  // Inside radius -> alpha = 1
            }

            operator==(alpha); // Assign back to patch field
        #};
        codeInclude     #{
            #include "vectorField.H"
        #};
        codeOptions     #{
            -I$(FOAM_SRC)/OpenFOAM/lnInclude
        #};
    }
    outlet
    {
        type            inletOutlet;
        phi             phi.argon;
        inletValue      uniform 0;
        value           uniform 0;
    }
    walls
    {
        type            zeroGradient;
    }
}


// ************************************************************************* //
